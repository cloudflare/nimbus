---
title: Integrity MDX
---

<Aside title="Wrapper">

😀 [Markdown guide](/integrity/plain/)

```tsx
const example = <Unclosed>;
<a href="/untouched">{notAnExpression}</a>
```

</Aside>

::::note[Outer]

:::tip[Say "hello"]
Nested **body** with `inline code`.
:::

::::

<Render file="example" />


:::note[When `name` and `class_name` differ — [guide][plain]]

```json
{ "name": "COUNTER_DO", "class_name": "CounterAgent" }
```

:::

[plain]: /integrity/plain/
