---
/**
 * Callout — backwards-compatible alias for Aside.
 * Prefer using <Aside> in new code.
 */
import Aside from "../aside/Aside.astro";

interface Props {
  variant?: "note" | "tip" | "caution" | "danger";
  title?: string;
  class?: string;
}

const { variant, title, ...rest } = Astro.props;
---

<Aside type={variant} title={title} {...rest}>
  <slot />
</Aside>
